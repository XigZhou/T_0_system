# 腾讯云服务器操作约定

## 连接默认值

- 用户提到腾讯云服务器、远程主机或部署时，优先使用本机已配置的 SSH 别名或既有连接方式。
- 只有在目标主机、用户名或目标目录不明确时，才做一句简短确认。

## 敏感信息处理

- 不要把密码、私钥、临时连接串写入仓库。
- 优先复用本机 SSH 配置、agent 转发、现有环境变量或历史可用连接方式。

## 远程执行规则

- 遇到递归删除、高风险重启、数据库变更等破坏性远程操作前，先和用户确认。
- 如果存在歧义，要明确说明当前要操作的是哪台主机或哪个别名。
- 远程操作后要检查结果是否符合预期，而不只是命令有没有执行成功。
